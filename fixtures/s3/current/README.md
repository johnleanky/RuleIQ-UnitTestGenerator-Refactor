# Current ScenarioGroup revision 1.3 sources

Eight synthetic Author fixtures freeze explicit expected-value types and payload-to-page bindings under DEC-029. They are independent immutable source examples, not a Generator upgrade facility. Earlier revision-1.2 and revision-1.1 fixtures remain unchanged historical regressions. PrimaryPage is explicitly evidenced in these sources, never a runtime default. The two-row fixture retains the separate Author formation described in ../projection_sources/README.md.

The raw-source correction suite varies explicitly supplied Author facts for typed values, PAGE arguments and mock wrappers, then validates the complete raw ledger before projection. These synthetic vectors prove transport and projection preservation, not semantic execution of live Pega rules. Runtime source acquisition remains with Scenario Author.
