# S5 Static Integration Fixtures

The source payloads come from unchanged fixtures/s3/current/ and represent already audited Author output. scripts/scenario_author_protocol.py models only storage/handoff/terminal response handling. scripts/s5_fixture_runtime.py connects unchanged S3 Generator and S4 Validator oracles through local immutable records and full schemas; it is not a Pega tool implementation.

Run `python -B scripts/validate_s5_integration.py` for16named integration groups. Deliberate candidate faults occur only after an unmodified full source/schema audit and declare whether the corrupted candidate remains schema-valid. Real Validator reports drive every repair/pruning path; terminal malformed-report fixtures test Author's boundary separately. Final surviving candidates receive an additional source audit. The shared budget is tested across semantic pruning and projection repair, including exhausted and failed-write outcomes.

product-deltas.json records the reviewed current runtime changes. reverse-matrix.json maps all150original obligations and retains S2's exact Author half plus current S3/S4 destinations. current-role-additions.json records36specific checks in effective prompts that historical dispositions omitted; the gate validates exact source anchors, role census, all destination references and Markdown agreement, with four negative mutations. preservation-manifest.json pins85unchanged baseline files to d4061046650c87b9da2c4f87141f23aa2602f082. The product artifact manifest is created only after independent design PASS permits product implementation.

The complete design/artifact gate runs historical regressions in an isolated local fixed-S4 checkout and separately checks current products, code integrity and flows. `--skip-historical` means historical checks were not rerun; only use it for focused unchanged-baseline rechecks after a recorded full run. No live Memory/Knowledge/Pega, LLM reasoning or target selection is performed.
