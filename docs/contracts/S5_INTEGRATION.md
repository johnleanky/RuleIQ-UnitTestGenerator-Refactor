# S5 Integration Contract and Evidence Boundary

Status: VERIFIED implementation after independent whole-stage gpt-6-astra/xhigh PASS and root reconciliation on2026-09-08; S5 and all repository roadmap stages are CLOSED. See the [acceptance report](../execution/design/S5-acceptance-report.md).

## DEC-reference clarification under DEC-032

VERIFIED — on2026-09-08 /root/review_ipm_cleanup actual gpt-6-astra/xhigh returned PASS for the clarified DEC cleanup, with no remaining actionable findings. Root reconciled25removed DEC references/16closed editorial substitutions, all five product hashes and fixed-closure reconstruction, both decoded export pairs and unchanged outside-prompt metadata,83protected files,150matrix rows,106Author IDs/132anchors and36consumer anchors. The complete S5 gate passed locally and independently (16integration groups, three annotation/ten artifact/six matrix controls, fixed historical S4/S3 gate including35producer groups and180correction regressions); ten additional independent DEC-reinsertion/baseline-drift probes were rejected. The reviewer-reported stale DEC-032 scope text was corrected and rechecked. Criterion15 is VERIFIED; S5 is CLOSED. All19project paths remain uncommitted, .idea/ is untouched, and no commit/push or Pega operation occurred. The separate two Validator behavior findings remain unfixed.

The following implementation entry records its pre-review checkpoint; the preceding PASS is the current verdict.

IMPLEMENTED_NOT_VERIFIED — the user clarified that remaining DEC references such as [DEC-026] must also be removed. Root removed25references (Main10, Generator12, Validator3) using16closed editorial substitutions that preserve operative requirements. Both exports are synchronized only inside pySystemPrompt. All five pre-cleanup products remain pinned to2c2fc19, current hashes/Author anchors are updated, and83other baseline files remain fixed; the Validator pair is now an explicitly authorized annotation-only delta. Full local validation and independent gpt-6-astra/xhigh review are pending. No commit/push, Pega runtime or unrelated Validator behavior fix is included.

The current closed editorial substitution list in [the builder](../../scripts/build_s5_artifacts.py) removes decision identifiers and replaces grammatical subjects with the existing contract or explicit revision. Actual types, limits, modes, signatures, schemas, routing and source-proof boundaries are retained. The current preservation census is83fixed files plus five exactly reconstructed products; previous85-file/unchanged-Validator statements below describe the preceding IPM-only checkpoint.

## Annotation-only follow-up under DEC-032

The initial S5 closure below describes commit2c2fc19. The user subsequently requested removing IPM identifiers from runtime prompts. The [annotation delta catalog](../../fixtures/s5/prompt-annotation-deltas.json) records27exact nonsemantic replacements; all operational text remains unchanged. The [external Author anchors](../../fixtures/s5/author-instruction-anchors.json) retain all106IDs and132section/quote locations, recovered from the tagged closure text and checked against clean current sections. Reverse-matrix destinations now use those headings. The builder applies the original S5 overlay first, then this bounded cleanup; historical S2–S4 files remain fixed. The Generator export is synchronized inside pySystemPrompt only. This follow-up is VERIFIED after the complete corrected gate, focused independent gpt-6-astra/xhigh PASS and root reconciliation. The annotation allowlist rejects operational insertions/deletions and pre-cleanup reconstruction must exactly match2c2fc19; three dedicated negative controls cover these protections. No behavioral fix, commit/push or Pega import is included.

## I1. Current authority and bounded corrections

S1 caller contracts, ScenarioGroup revision1.3, the closed Generator/Validator reports and DEC-001–030 remain authoritative. DEC-031 resolves two demonstrated cross-stage inconsistencies without changing payload grammar or downstream schemas:

- S1 section5 allows platform metadata in Memory results. Required named fields retain exact spelling, types and success/error invariants. Main Sections1A/13's rejection of any extra field and the same statement in protected SCENARIO_GROUP_V1.md:17 are historical over-restrictions superseded only for envelope extras. The source contract stays byte-identical as historical evidence; this current integration interpretation and effective Author text take precedence on that one issue. Generic response text does not replace required fields.
- The full SIM grammar remains exact, including sig and typed params. Generator proves these and payload bindings against immutable source. Validator verifies observable fields, unique physical association and shared When identity; DEC-030 forbids inventing separate physical carriers or claiming absent-source proof. Bounded replacement propagates that already accepted boundary into Main and Generator embedded source references.

The [exact delta catalog](../../fixtures/s5/product-deltas.json) fixes old/new text, occurrence count, original rows and rationale. Legacy names in prohibitions or overridden historical text are replaced by generic transport descriptions, preserving prohibition and semantic/downstream locks. No old transport becomes executable. A self-contained existing GeneratorRunReport schema is appended to Author for terminal response validation, together with observable G9 status/outcome/count/current-coordinate checks and binding to original CaseID/ordered source/group/Scenario identities; it authorizes no candidate JSON parsing, UTC schema call or semantic inference. Section1A's fixed external status and error mapping remains unchanged.

## I2. Effective artifacts and preservation

The five effective deliverables are Main_Agent_Prompt.txt, UnitTestGenerator_Prompt.txt, UnitTestGenerator.txt, Validator_Prompt.txt and JsonValidator_tool.txt. Both Generator and Validator pairs must decode exactly. Current product deltas include the authorized IPM and DEC reference cleanup in all three prompts. Both exports change only their single pySystemPrompt bodies; every unrelated export field remains baseline bytes. Main_Agent.txt is excluded from parity/edits and separately hash-protected.

Historical S2/S3/S4 contracts, fixtures, schemas, oracles, builders and tests stay fixed. Their complete S4 gate runs in an isolated local checkout of d4061046650c87b9da2c4f87141f23aa2602f082. A historical PASS cannot establish current integration: current checks separately require exact reviewed overlay reconstruction, protected-file census, both-pair parity, absence of all three legacy names across five deliverables and connected flows through unchanged implementations.

## I3. Reverse ownership audit

Every original row is preserved verbatim in the [reverse matrix](../execution/design/S5-instruction-reverse-audit.md) and [machine catalog](../../fixtures/s5/reverse-matrix.json). Each retained row has one accountable authority and separately named effective responsibilities. S2's exact Author half is retained; current-role-additions.json records36distinct consumer checks omitted by historical dispositions with exact current section/quote evidence, and the gate validates all150current destinations and Markdown/catalog agreement; S3 owns projection/source validation and repair; S4 owns candidate checks/report production. Shared checks do not permit consumers to derive new semantic facts. A single source clause can require multiple different actions; accountability does not mean those distinct actions run in one agent.

Overridden historical candidate instructions in Main remain non-executable under Sections1/1B/5–12 locks. Original semantic facts/gates remain Author responsibilities; downstream projection descriptions provide preservation context only. The exact Source->S2/S3/S4->current mapping plus bounded text restoration must reveal any dropped obligation; scope labels alone are not completeness proof. DEC-031 and accepted earlier decisions justify the one obsolete Validator transport row and all literal-name replacements. Original-clause review is independently required in addition to machine census.

## I4. Connected static caller model

The [Author adapter](../../scripts/scenario_author_protocol.py) starts with an original GetCaseData context and already authored final payload fixtures. Its shared source parser verifies the complete existing grammar before any writes. It does not simulate crawling, semantic acquisition or arbitrary Author reasoning. It writes each ScenarioGroup once in original group order, validates the S1 envelope and hands off all returned opaque UUIDs exactly once only after complete success.

The [fixture runtime](../../scripts/s5_fixture_runtime.py) connects this boundary to unchanged generator_protocol.run, generator_projection, validator_protocol.run, full candidate/report schemas and independent source/candidate audit. Memory doubles are local immutable records separated by CaseID, Type and UUID. They are test fixtures, not external Pega implementations. Per-agent attempted calls and resolved storage addresses are recorded; these are not Pega logs. The Author adapter consumes the unchanged full GeneratorRunReport schema, rejects visible contradictions and wrong known handoff identities without fetching candidate bytes or claiming independent proof of Validator OK, and emits only the existing two-field external object; Completed and PartiallyCompleted map to Completed, Failed maps to the fixed generation error, malformed response to the fixed invalid-response error.

Every ordinary projection passes the existing bidirectional source/full-schema audit before optional deliberate candidate corruption. Fault injection is explicit and precedes storage of that immutable candidate version; a corrupted candidate is never described as source-audit-verified. Validator inspects the actual stored version and generates its real report. Generator prunes/remaps or reconstructs from unchanged source using that report; every final surviving successful candidate receives a fresh full source/schema audit. UUID/address faults exercise named failure envelopes and exact caller intent; a malicious backend returning undetectably substituted payloads is not proven safe by a UUID-less response contract.

## I5. Required flow evidence

Current tests cover both-family success, multiple When groups and source-limited partial success; write/uncertain-write failures before partial handoff; malformed source before writes; full terminal report mapping and13schema-valid contradiction mutations as objects/raw JSON, plus legitimate duplicate-UUID invocation failure; ordinary schema/projection repair with fresh UUID; isolated When Scenario pruning and row remapping; shared group rejection mixed with another unit's projection error; all-unrepresentable/all-rejected failures; exhausted two-repair budget and failed repair write; wrong CaseID/Type/UUID without latest fallback; missing/malformed tool and Knowledge results; record-type isolation and reused candidate UUID; adversarial call-log monitor controls.

Assertions must prove current UUIDs, no retry or extra tools, source-byte/history preservation, current Scenario/decision coordinates, narrow shared scope, final output visibility and fixed status mapping. Deliberately malformed Generator reports are boundary tests, not canned successful validation flows. Actual schema/report/code sources are unchanged; all runtime validation routes otherwise come from the real S4 producer.

## I6. Completion limits

Run python -B scripts/validate_s5_integration.py and the complete python -B scripts/validate_s5_design.py gate. Design-only mode evaluates proposed artifacts in memory while actual products must remain byte-identical to the activation baseline. Product mode verifies on-disk artifacts and their hash manifest. All source facts and original semantic behavior retain historical instruction/parser evidence and independent source-clause review; the tests do not interpret arbitrary free-text Knowledge or execute an LLM.

External tool backing, target selection, named-reference resolution, Pega import/runtime and live semantic execution remain outside this repository task. S5 closure requires independent whole-stage gpt-6-astra/xhigh PASS plus root reconciliation. The original stage request did not authorize a commit or push. The subsequent 2026-09-08 user request authorizes a local S5 closure commit; push remains outside scope.
