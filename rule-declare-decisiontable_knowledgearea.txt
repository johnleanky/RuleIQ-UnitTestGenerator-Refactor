Marker kinds: F=Field,V=Value,L=List,M=Mapping,A=Algorithm,S=Semantic,P=Protocol.
Line labels: P=Purpose; M=Mapping; I=Invariant; A=Algorithm/Protocol; S=Semantic; X=Example/semantic witness; R=Related markers.
Canonical source = heading + P/M/I/A/S. Examples clarify only.
Coverage: metadata KDT.1; invocation KDT.2-KDT.3; structure KDT.4; row evaluation KDT.5-KDT.7; parameters/context KDT.8; advanced behavior KDT.9; nested dependencies KDT.10; evidence KDT.11; integration audit KDT.12.

KDT.1|S|MetadataAndCrawlerKey
P: Stable Rule-Declare-DecisionTable identity.
M: pxObjClass="Rule-Declare-DecisionTable"; apply-to key=pyClassName; rule-name key=pyPurpose.
M: Canonical dependency key=`DTable@<pyClassName>@<pyPurpose>`.
M: Synthetic sourceReference={pxRuleObjClass:"Rule-Declare-DecisionTable",pyRuleName:<pyPurpose>,pxRuleClassName:<pyClassName>}.
I: Both class and name are required. Same pyPurpose on different classes is a different dependency.
I: KDT supplies Decision Table identity and the synthetic sourceReference only. The caller materializes the final request through the canonical synthetic non-RUF materializer and gate.

KDT.2|L|StandardInvocationPatterns
P: Detect a Decision Table invoked by a RUT or fetched dependency.
M: @DecisionTable.ObtainValue(tools,PageContext,TableName[,AllowMissingProperties]) and @(Pega-RULES:DecisionTable).ObtainValue(...) -> name=arg3; context=arg2.
M: @Utilities.pxEvaluateDecisionTable(PageContext,TableName) and @pxEvaluateDecisionTable(...) -> name=arg2; context=arg1.
M: @evaluateDecisionTable(TableName) -> name=arg1; context=current declarative page.
A: Parse arguments with balanced parentheses. Normalize a statically known name by trimming and removing one matching outer quote pair.
I: Do not infer a table name from a dynamic property/parameter expression. Structured pxRuleReferences identity may prove an otherwise ambiguous invocation.

KDT.3|A|InvocationContextResolution
P: Resolve the Decision Table apply-to class from the invocation page.
A: Primary/current step/current declarative page -> current holder execution class. Named page -> pyPagesAndClasses class. Embedded Page/PageList/PageGroup -> traverse Rule-Obj-Property definitions left-to-right and use final pyPageClass.
I: Context proof is occurrence-local. The first pxEvaluateDecisionTable argument is a page-context token, not automatically an apply-to class.
A: If class is unresolved, keep DTable row CTX and request provider properties; never substitute the RUT class by guess.

KDT.4|F|CoreRuleStructure
P: Fields used by first-match, evaluate-all-rows, scalar-return, row-action, default-action, and allowed-result property-set Decision Table forms.
M: input columns -> pyColumns[].
M: column expression/property -> pyColumns[].pyProperty.
M: column default operator -> pyColumns[].pyDefaultOperator.
M: range participation -> pyColumns[].pyUseRange and pyColumns[].pyUseRangeMax.
I: A column with pyUseRange=true is the range-start/lower-bound column and its paired column with pyUseRangeMax=true is the range-end/upper-bound column for the same input expression. For every row index, read both columns' aligned pyCondition[row] values at that same index. Neither boundary column is metadata-only, and pyUseRange=true never makes a populated lower-bound cell wildcard.
M: row cells per column -> pyColumns[].pyCondition[].
M: split/OR alternatives -> pyColumns[].pyOrConditions[] with pyRowNum and pyOrCondition[]. During initial KDT.4 transcription, preserve a lossless immutable inventory entry for every active OR container: exact column index, container index/path, literal stored pyRowNum including -1, and exact alternatives in stored order. No active container may be omitted, duplicated, reordered, or reconstructed later; this inventory is internal execution proof, not a public JSON field or a new record type. Associate an OR container with an executable row exclusively by its stored integer pyRowNum. That stored value is already the zero-based executable-row index in RuleJSON: copy it verbatim, never add or subtract 1, never normalize it from a UI/display row number, and never replace it with the pyOrConditions list position. Stored pyRowNum=1 matches row index 1, the second executable JSON row, never row index 0. pyRowNum=-1 is a structural sentinel for no OR row and matches no executable row; ignore only that container during row evaluation. A missing or non-integer pyRowNum with nonblank alternatives makes only the affected cell mapping UNKNOWN and must never default to row 0.
M: row results aligned by row -> pyResults[].
M: property-action targets -> pyPropertyColumns[].pyProperty.
M: property-action default operator -> pyPropertyColumns[].pyDefaultPropertySetOperator.
M: property-action values aligned by row -> pyPropertyColumns[].pyPropertyValues[].
M: no-match property actions -> pyDefaultResultPropSet[] with pyPropertiesName and pyPropertiesValue.
M: no-match behavior -> pyDefaultAction and pyDefaultResult.
M: evaluate-all mode -> pyDelegatedRestrictions(default).pyEvaluateAllRowsStr.
M: scalar-return enablement -> pyDelegatedRestrictions(default).pyAllowedToReturnValuesStr.
M: allowed scalar result expression -> pyAllowedValues[].pyAllowedValue.
M: allowed-result property-action presence -> pyAllowedValues[].pzIsAllowedResultsPropSetExist.
M: allowed-result property actions -> pyAllowedValues[].pyPropertySet[] with pyPropertiesName, pyPropertiesValue, and pyExpressionBuilder.
M: parameter definitions -> pyParameters; named page declarations -> pyPagesAndClasses.
A: Before scanning any condition, result, action, expression, or nested dependency, inspect only pyDelegatedRestrictions(default).pyEvaluateAllRowsStr and pyDelegatedRestrictions(default).pyAllowedToReturnValuesStr and create one immutable invocation-occurrence record: DTM|source=<DTable@Class@Name>|page=<invocation page>|mode=<FIRST_MATCH|EVALUATE_ALL>|scalar=<ENABLED|DISABLED>|active=<fields>|inactive=<fields>|proof=<flags>.
A: Normalize pyEvaluateAllRowsStr case-insensitively: boolean/string true means EVALUATE_ALL; false, blank, or absent means FIRST_MATCH. Normalize pyAllowedToReturnValuesStr independently: only boolean/string true enables scalar return; false, blank, or absent disables it. EVALUATE_ALL always wins and uses scalar=DISABLED. FIRST_MATCH uses scalar=ENABLED only when pyAllowedToReturnValuesStr is true; then activate conditions, row actions, default property actions, pyResults, pyDefaultResult, allowed values, and allowed-result property sets. FIRST_MATCH with scalar=DISABLED and EVALUATE_ALL both keep conditions, pyPropertyColumns, and pyDefaultResultPropSet active while making pyResults, pyDefaultResult, pyAllowedValues[].pyAllowedValue, and allowed-result pyPropertySet[] inactive before dependency scan. FIRST_MATCH still selects only its first matching row when scalar is disabled.
I: DTM is the sole execution-mode and scalar authority. Inactive fields may be noted only as ignored evidence; never parse, evaluate, dependency-scan, seed from, propagate, expose as execution outputs, or serialize them. Later phases must not re-read mode or inactive fields from RuleJSON.
I: Preserve stored input-column, row, property-action-column, matched-row, and allowed-result property-action order. Do not sort, collapse blank cells, or align rows by display text.
I: pyColumns are reads/inputs. pyPropertyColumns and pyDefaultResultPropSet are writes/outputs, not additional Decision Table result dimensions by themselves. Only a reachable pyPropertyColumns action whose effective operator is +=, -=, *=, /=, or %= additionally reads its own target before writing it; that target is a read-before-write input and an output for that execution.

KDT.5|A|FirstMatchEvaluation
P: Canonical evaluation for an ordinary first-match table with scalar return values, property actions, or both.
A: Execute this algorithm whenever the invocation DTM says mode=FIRST_MATCH, regardless of scalar state. Read only DTM.active fields.
A: 1 resolve invocation page and parameters. 2 evaluate each pyColumns[].pyProperty in that context. 3 inspect rows in stored order using a zero-based row index. 4 for each column, inspect every pyOrConditions container's own stored integer pyRowNum and select only the container whose pyRowNum equals the current row index; never substitute the container's list ordinal, and skip pyRowNum=-1. 5 if a matching OR entry exists, ignore the aligned pyCondition completely whether it is blank or populated; evaluate every nonblank pyOrCondition alternative through KDT.6 in stored order and combine them with OR. A seed candidate for that cell must satisfy at least one alternative, and when multiple deterministic seed choices are possible choose a value satisfying the first nonblank satisfiable alternative in stored order. If the matching OR entry contains no nonblank alternatives, the cell is wildcard true. 6 only when no matching OR entry exists, evaluate the pyCondition value at that same row index through KDT.6; a blank aligned pyCondition is then wildcard true. Never shift pyCondition, pyResults, or pyPropertyValues alignment because another row uses an OR container. 7 combine resolved column cells with AND. 8 the first matching row selects the aligned pyResults entry only when DTM.scalar=ENABLED and selects the same row index in every pyPropertyColumns[].pyPropertyValues list regardless of scalar state. 9 apply every nonblank aligned row action through KDT.6 in stored property-column order. 10 only after an enabled scalar result is determined, apply its unique active allowed-result property-set actions through KDT.9. 11 if no row matches, apply KDT.7.
I: A row-aligned pyOrConditions entry always has precedence over pyCondition for that column and row in every execution mode. Its alternatives replace, not supplement, the aligned pyCondition. When it contains nonblank alternatives, the cell is false if the actual input satisfies none of them; an arbitrary non-member seed is invalid and cannot select the row. Evaluate pyCondition only when no pyOrConditions entry with pyRowNum equal to the current row exists.
I: When a row contains a pyUseRange/pyUseRangeMax pair, evaluate that pair once as one AND constraint after resolving both aligned boundary cells for the current row. A nonblank start boundary remains active even when another condition in the row is wildcard. Never skip the start boundary, borrow a boundary from another row, or validate only the max boundary.
I: Stop at the first matching row. Use KDT.9 when DTM proves EVALUATE_ALL; do not infer mode again from raw fields here.
I: A missing or disabled scalar return does not suppress property actions for the selected row.
I: The first-match outcome is the scalar return only when enabled, plus the ordered row-action vector; include the ordered allowed-result property-action vector only when scalar is enabled.

KDT.6|S|CellResultAndPropertyActionExpressions
P: Evaluate native comparisons, scalar results, and property actions without inventing operator semantics.
M: Native comparison operators are =, !=, <>, ^=, >, <, >=, and <=. Normalize <> and ^= to !=. Parse two-character operators before one-character prefixes.
M: pyDefaultOperator supplies the column operator when the cell does not encode an override. An explicit stored cell operator overrides only that cell.
M: pyUseRange and pyUseRangeMax identify the starting/lower and ending/upper boundary columns for one input expression. For each row, apply each boundary's stored/default operator directly to the current typed input and that row's aligned boundary value: for example, lower operator `>` means `currentInput > lowerValue`, and upper operator `<=` means `currentInput <= upperValue`. Preserve strict versus inclusive semantics exactly and require both populated boundaries to match. A blank boundary is unbounded only on that side; it does not blank or suppress the paired boundary.
M: pyDefaultPropertySetOperator supplies the pyPropertyColumns action operator. Normalize stored =, +, -, *, /, and % forms and explicit cell shorthands =, +=, -=, *=, /=, and %=. For the pyPropertyValues entry aligned with the matched row, an explicit cell shorthand is the effective operator; otherwise use the property-column default.
S: Blank pyCondition means no restriction for that input column/row only when no row-aligned pyOrConditions entry applies. When a matching pyOrConditions entry exists, ignore pyCondition regardless of whether pyCondition is blank or populated.
S: Blank or missing pyPropertyValues means no write for that target in that row. Clearing a property requires an explicit empty-string expression; never infer a clear from a blank action cell.
S: pyColumns[].pyProperty, active pyResults values, and pyPropertyValues may be executable expressions, property references, parameters, literals, functions, or nested rule calls. Inactive pyResults are opaque ignored evidence, not expressions to parse.
A: Resolve both sides of property-to-value and property-to-property comparisons in invocation context. Fetch the Rule-Obj-Property definition and compare using the evidenced Pega type and formatting rules whenever type affects deterministic conversion or ordering.
A: For a stored range, evaluate the current row's lower and upper boundary expressions in invocation context and AND the two comparisons using their evidenced inclusive/exclusive operators. When deterministic seed generation must satisfy one or more selected Integer ranges, intersect all required bounds first and choose the lowest representable Integer inside that intersection: a strict lower bound `> L` starts at `L+1`, an inclusive lower bound `>= L` starts at `L`; reject the candidate if it violates any upper bound. Never use the literal strict lower boundary itself as a satisfying seed. For Decimal or other ordered types, choose an interior value only when fetched type precision/scale and conversion evidence make it deterministic; otherwise mark only the affected selection/output UNKNOWN.
A: `contains`, `starts with`, `ends with`, and similar text predicates are not native Decision Table comparison operators. Execute them only when RuleJSON stores an explicit expression or function call; bind `<current-value>` to the current column value exactly as the stored expression requires.
A: For each executing selected-row pyPropertyColumns action, resolve pyProperty as the destination in invocation context and create exactly one ordered record: DTA|id=<id>|scenario=<id>|row=<n>|order=<n>|page=<page>|target=<path>|op=<op>|beforeSource=<earlier_write|scenario_input|scenario_constraint|increment_seed|unresolved>|before=<value>|operand=<value>|after=<value>|status=<FINAL|UNKNOWN>. Evaluate the aligned pyPropertyValues expression once against current state and store it as operand. "=" assigns. Numeric +=, -=, *=, /=, and %= use the current typed target value. For Text/Identifier targets, += concatenates the text operand; honor any other documented text-append encoding only when fetched evidence proves it. Fetch Rule-Obj-Property when type or formatting affects evaluation.
A: For a compound DTA, resolve before strictly as: latest deterministic earlier write or FINAL DTA -> existing explicit caller-provided input on the same invocation page -> the single exact typed value forced by the selected-row constraints and an exact downstream constraint supplied for that page-qualified target. The third source remains `scenario_constraint`; range/inequality, wildcard, multiple OR alternatives/values, multiple satisfying values, and an unconstrained target are not exact. Only for effective += when those sources fail, create `increment_seed`: choose a caller-constraint-compatible before value when requested, otherwise use "Init" for Text/Identifier or 0 for an evidenced numeric scalar. Do not generate this fallback for -=, *=, /=, %=, "=", unsupported types, blank cells, or non-executing actions.
A: Materialize every scenario_constraint or increment_seed directly in final RuleCode.pySetupPages on the actual invocation page before the first executing action. The JSON object must physically contain the page-qualified key and exact value before DTA.after may support an assertion. Compute += after from that serialized value and ordered operands. The setup value is a runtime input, not an assertion or Scenario dimension. In evaluate-all, later matched rows read the preceding FINAL DTA.after and never reinitialize it.
I: If no permitted before source exists, set beforeSource=unresolved, before=UNKNOWN, after=UNKNOWN, status=UNKNOWN and emit OMIT|reason=unresolvable_runtime_input for only that property result and dependent downstream behavior. Never compute after without before. Division/modulo by zero, unknown type, or an unresolved operand likewise makes only the affected DTA and dependent behavior UNKNOWN.
I: If an executing compound DTA target is simultaneously forbidden by the caller's selected-execution seed constraints, the execution proof is contradictory. Re-evaluate the supplied constraints, matched rows, DTE, and DTA. Never silently remove the before seed while retaining a computed after value.
I: This read-before-write seed exception applies only to reachable pyPropertyColumns actions. Do not extend it to pyDefaultResultPropSet, allowed-result pyPropertySet, or preset-property actions. A blank or missing pyPropSetDefaultValue does not initialize the target.
I: Evaluate expressions in invocation context. Never compare or return UI display text when RuleJSON contains an executable expression.
I: If an input operator, range encoding, scalar result, or row-selection expression cannot be interpreted from evidence, the row result is unknown.
I: If only one DTA target, operator, before, operand, or typed operation is unresolved, mark that DTA and behavior depending on it unknown; preserve other independently FINAL DTA writes.

KDT.7|S|DefaultBehavior
P: Resolve no-match behavior.
M: ordinary return action -> evaluate pyDefaultResult only when DTM says FIRST_MATCH, scalar=ENABLED, and the field is active.
M: row-aligned default property actions -> process active pyDefaultResultPropSet[] through KDT.6 in stored order, resolve each nonblank pyPropertiesName target, evaluate pyPropertiesValue, and apply the corresponding pyPropertyColumns operator or explicit value shorthand. In EVALUATE_ALL this is the only no-match result behavior.
M: callbase -> invoke the evidenced base Decision Table behavior; this is an executable dependency and must be closed before exact output.
M: redirect or other default action -> follow only explicit RuleJSON/KnowledgeArea semantics.
S: Blank or missing pyPropertiesValue means no default-path write; an explicit empty-string expression performs a clear.
I: Missing/blank active scalar default does not imply empty string, false, or null without runtime evidence. Whenever DTM.scalar=DISABLED, scalar is NONE and never a value inferred from pyDefaultResult or another inactive return field.

KDT.8|A|ParametersPagesAndAllowMissing
P: Bind runtime inputs used by the table.
A: Resolve parameter values in fixed order: explicit invocation/current parameter-page evidence -> inherited caller parameter when enabled/evidenced -> empty/null only when runtime semantics prove it.
S: ObtainValue evaluates against its PageContext argument. pxEvaluateDecisionTable evaluates against its first page-context argument. evaluateDecisionTable uses current declarative context.
S: AllowMissingProperties affects missing-property handling; it does not change table identity.
I: Seed only deterministic inputs required by an evaluated candidate; never seed an output merely to multiply results. Output seeds are permitted only as a KDT.6 exact scenario_constraint or reachable += increment_seed. A scenario_constraint remains exact; only an increment_seed may use a requested caller-compatible before value and otherwise "Init" for Text/Identifier or 0 for numeric. Without a permitted before value, omit the affected output instead of inventing a seed.

KDT.9|A|EvaluateAllRowsAndAllowedResults
P: Execute evaluate-all rows and scalar-result property sets deterministically while localizing unsupported behavior.
A: When DTM says EVALUATE_ALL, set scalar=NONE before row evaluation. Inspect every active row in stored order and determine whether it matches by applying KDT.5 steps 3-7 unchanged. The row-aligned pyOrConditions precedence rule is mode-independent: a matching OR entry replaces pyCondition even when pyCondition is populated. For each matching row, create and apply every nonblank aligned pyPropertyColumns DTA in stored property-column order against the state produced by earlier FINAL DTA records. Record the ordered matched-row list and cumulative final property writes. Never read pyResults, pyDefaultResult, pyAllowedValues[].pyAllowedValue, or allowed-result pyPropertySet[] in this mode; they are inactive even when populated.
A: If no row matches in EVALUATE_ALL, execute only active pyDefaultResultPropSet actions in stored order. Do not invent a scalar default and do not consult inactive return/default/allowed-result fields.
A: Only when DTM says FIRST_MATCH with scalar=ENABLED, after computing the active scalar return, evaluate active pyAllowedValues[].pyAllowedValue expressions and find the unique entry equal to that typed scalar value. When its property-set presence is proved, execute its active pyPropertySet[] entries in stored order after selected-row actions. Resolve pyPropertiesName, evaluate pyPropertiesValue or pyExpressionBuilder in invocation context, and apply the explicitly stored assignment/compound semantics through KDT.6.
S: An allowed value without property-set actions is a constraint on legal scalar return values, not a scenario, branch, or side effect.
I: Duplicate evaluated allowed values matching the scalar outcome, or an unknown allowed-value/property-set expression, makes only the associated allowed-result writes and behavior depending on them unknown. Preserve a deterministic scalar return, selected-row actions, and unrelated property outputs.
I: In EVALUATE_ALL, initialize a pyPropertyColumns compound target at most once and only from a valid KDT.6 before source. Repeated writes, overwrites, compound operations, text appends, and Page List append/list effects are sequential; every later DTA reads the state produced by earlier matched rows. Assert only the final cumulative value after all matched rows and downstream RUT execution.
I: Preset properties, callbase, redirect/circumstance behavior, dynamic table identity, and other actions without complete RuleJSON/KnowledgeArea semantics remain advanced gaps. Localize unknown status to affected outputs and dependent behavior.

KDT.10|A|NestedDependencyScan
P: Close dependencies inside a fetched Decision Table before closing its DTable row.
A: Create DTM first. In every mode, scan active pyColumns expressions, condition/OR/range expressions, pyPropertyColumns targets and values, active pyDefaultResultPropSet pairs, parameters, Pages & Classes, active preset/callbase/redirect behavior, and other active expressions for functions, When rules, Decision Tables, Data Pages, properties, and executable rules. Only when DTM says FIRST_MATCH with scalar=ENABLED scan active pyResults, pyDefaultResult, pyAllowedValues[].pyAllowedValue, and allowed-result pyPropertySet target/value/expression. Whenever DTM.scalar=DISABLED, do not parse or dependency-scan those inactive fields even if they contain expressions or nested calls.
I: A fetched table is not CLOSED until DTM exists and every dependency discovered from active fields has a D row or exact CLOSED key link. Inactive fields create no D row and do not prevent closure.
I: A DecisionTable/Utilities RUF contract closes only the function contract, not a concrete nested Decision Table invocation.

KDT.11|P|EvidenceAndAssertionImpact
P: Bind Decision Table evaluation to branch and value evidence.
A: For every invocation and evaluated execution candidate, create exactly one record after active evaluation: DTE|scenario=<id>|source=<DTable@Class@Name>|mode=<FIRST_MATCH|EVALUATE_ALL>|matched=<rows/default>|scalar=<value|NONE|UNKNOWN>|consumerTargets=<paths>|actions=<ids>|cellProof=<ordered selected-cell proof>|status=<FINAL|UNKNOWN>. The `scenario` token is only a correlation id for DTM/DTA/DTE evidence; it does not authorize or count a serialized Scenario. DTE references ordered DTA/property-effect ids and carries its mandatory internal cellProof; it is the sole Decision Table execution-evidence output supplied to the caller workflow. cellProof is internal evidence, not a public JSON field or separate record type. No later phase may recompute the Decision Table from RuleJSON.
A: Before assigning status=FINAL to a DTE, validate every semantically evaluated row through KDT.5/KDT.9 using the exact candidate page-qualified setup, parameter, and simulation inputs. For every evaluated row and active column, preserve one ordered immutable cellProof item containing the column index, evaluated zero-based row index, source=OR(inventoryPath=<exact container path>,rawPyRowNum=<verbatim stored integer>) or CONDITION(index=<row>), effective alternatives/cell, resolved column input value, and Boolean result. An OR source is permitted only when exactly one KDT.4 inventory entry in that column has rawPyRowNum equal to the evaluated row and must cite that entry. CONDITION is permitted only when none does; multiple matching containers without separate fetched semantics make only that cell UNKNOWN. Missing or inconsistent inventory or cellProof forbids DTE.status=FINAL. Derive each row result by AND of its complete active-column cellProof; never declare DTE.matched independently of those results. Re-check OR row association only from rawPyRowNum, never from the container's list position and never by adding or subtracting 1; pyRowNum=-1 associates only that container with no executable row and says nothing about other containers in the column. If any actual seed or simulation value fails its effective aligned condition, range, or row-specific OR alternatives, that row is not matched: do not copy its scalar result or actions into DTE, re-run ordered row/default evaluation, and rebuild the candidate. Whenever a row-aligned pyOrConditions entry exists, validate only its alternatives for that cell and ignore pyCondition completely; no non-member input can pass as wildcard in FIRST_MATCH or EVALUATE_ALL.
A: DTE mode/scalar must equal DTM. FIRST_MATCH scalar is the deterministic typed active result or UNKNOWN only when DTM.scalar=ENABLED; otherwise it is NONE. EVALUATE_ALL scalar is always NONE, even when inactive return fields are populated or active actions are UNKNOWN. DTE status is FINAL only when every outcome component required by a final claim is deterministic; otherwise localize UNKNOWN to the affected action/consumer while retaining independent FINAL effects.
A: For property P in a selected execution, a Decision Table proves a write only when its final DTE references a FINAL DTA whose page-qualified target equals P. Only such an executing DTA may make P a read-before-write input or create a setup obligation. Potential actions present in Decision Table RuleJSON, actions from non-selected rows, DTA records from other execution candidates, and reads of P are not write evidence and do not instantiate P.
I: If the selected final DTE references no DTA targeting P, the Decision Table preserves P's incoming existence and value state. Existence may then be proved only by independent setup, an evidenced pre-existing Scenario input, or another actually executed write outside that Decision Table invocation.
A: Record every direct and transitive caller target that attempts to consume the scalar result in DTE.consumerTargets. When DTE.scalar=NONE, report `inactive_evaluate_all_scalar` if DTM.mode=EVALUATE_ALL or `inactive_return_values_scalar` if DTM.mode=FIRST_MATCH with scalar disabled. Forbid any value, existence state, branch result, or setup seed derived from that scalar. Do not substitute empty, null, false, the last row result, or stale pyResults. Preserve independent FINAL DTA property effects.
A: Preserve invocation source, DTM/DTE, context and parameter proof, matched rows/default, every ordered DTA field, active dependency keys, and affected branch/property. Project only assertion-relevant compact DTM/DTE/DTA facts into existing ReasoningTrace/AssertionDecisionTrace; add no public JSON fields.
S: The DT-local result is the enabled typed scalar plus ordered final property effects in FIRST_MATCH, scalar=NONE plus those effects when FIRST_MATCH scalar is disabled, and scalar=NONE plus ordered matched-row evidence and the cumulative final property-effect vector in EVALUATE_ALL. Row/default identity alone does not distinguish an identical deterministic scalar/property-effect result.
A: KDT produces execution evidence only. A nested table exposes dependency candidates; a directly tested table exposes executable outputs. The caller alone owns coverage, test selection, grouping, and serialization.
A: When the caller workflow requests a DT candidate compatible with an exact downstream equality constraint whose target is the final value of an ordered deterministic chain of one or more executing += DTA records with no intervening overwrite, REMOVE, or containing-page deletion, inspect that constraint before using the canonical "Init"/0 fallback. If no higher-priority KDT.6 before source exists, perform inverse seed resolution. For an evidenced numeric scalar, require deterministic typed operands and compute candidate before = required final value - the ordered operand sum, preserving the evidenced numeric type and range. For Text/Identifier, require deterministic text operands, concatenate them in DTA execution order, require the exact required final value to end with that complete suffix, and use the remaining nonblank type-valid prefix as candidate before. An empty prefix is not a valid initialization.
A: Store a valid inverse result in the first DTA as beforeSource=increment_seed and before=<candidate>; every later DTA in the chain uses beforeSource=earlier_write. Materialize that exact first before value on the resolved invocation page, then re-run ordered Decision Table row evaluation and rebuild the complete DTA/DTE chain. Return the candidate only when the physical seed preserves the claimed matched rows and produces the exact constrained final target value. Caller-level compatibility and coverage remain the caller workflow's responsibility. If row selection or another DT constraint changes, discard the candidate and do not preserve its DTA/DTE or assertions.
I: Inverse seed resolution never overrides an earlier deterministic write, explicit Scenario input, or exact scenario_constraint. Do not backsolve unknown types or operands, non-exact/range/inequality constraints, mixed or non-+= action chains, chains with an intervening write/removal, or ambiguous Text suffixes. When no valid inverse exists, retain the existing fallback or localized OMIT behavior.
S: DT candidate identity includes mode, scalar state/value, ordered final property-effect vector, required page/parameter bindings, and active dependency status. DT result-equivalence uses mode, scalar state/value, and the final property-effect vector: row/default or allowed-entry identity, Evaluate All matched-row sequence/count, and inactive fields do not create another output class. Merge exact duplicate candidates with equivalent inputs, but retain alternative compatible inputs within their result class until the caller selects one.
I: Nested tables expose these candidates only as dependency evidence; direct Decision Table RUTs expose every distinct executable FINAL result class as an output. Neither statement authorizes serialized Scenarios or grouping inside KDT.
I: A pyColumns-only read is a seed, not an assertion. A reachable compound pyPropertyColumns target is both read-before-write input and output; other property/default/allowed-result writes are outputs only. For a direct DT, assert deterministic property actions on its output page even without scalar return; for a nested DT, assert only the final value after downstream overwrites.
I: Exact output requires DTM, FINAL DTE and FINAL affecting DTA records, deterministic inputs, known operators, and CLOSED active dependencies. Repair missing/inconsistent evidence or, after terminal proof, localize UNKNOWN/OMIT to only affected RX states and exact assertions while preserving unrelated FINAL outputs; raw RuleJSON is never a post-record fallback.
I: Before serialization, inspect the actual candidate RuleCode JSON. Every executing += with beforeSource=increment_seed must have the exact chosen value in pySetupPages on the invocation page: the caller-constraint-compatible value, otherwise "Init" for Text/Identifier or 0 for numeric. Recompute every dependent expected result from that serialized value. A missing or inconsistent key invalidates the dependent exact assertion until RuleCode is repaired; ReasoningTrace text is not proof.

KDT.12|A|PreSerializationIntegrationAudit
P: Verify that Knowledge-produced Decision Table records and their selected-row proof agree with the selected execution and final serialized RuleCode.
A: For every reachable invocation and selected execution, require exactly one DTM, exactly one DTE with complete cellProof, every DTA named by DTE.actions in recorded order, and CLOSED dependencies for all DTM-active fields. Caller execution, setup, property traces, assertions, omissions, and RuleCode must consume those records and must not re-read Decision Table RuleJSON to author or replace outcomes. The sole permitted post-record RuleJSON access is the literal transcription-integrity comparison below, limited to active pyOrConditions array shape/order, container pyRowNum and alternatives, and aligned pyCondition cells cited by CONDITION proof; it validates stored evidence and never computes, patches, or replaces an outcome.
A: Before row revalidation, compare the complete KDT.4 OR inventory one-to-one with every active fetched RuleJSON pyColumns[column].pyOrConditions[container]: exact array length, container path/index, literal pyRowNum including -1, alternatives, and stored order must match, with no missing or extra entry. Compare every aligned pyCondition cell cited by CONDITION proof with its literal stored value. Then require every DTE OR source to cite the matching verified inventory entry and every semantically evaluated row/active column to have exactly one cellProof item. Literal values must be identical: never add or subtract 1, normalize a UI/display row number, or substitute the container list position. This is an independent transcription check, not Decision Table recomputation. Any inventory, CONDITION, source, or proof mismatch invalidates cellProof, DTE, every DTA derived from its claimed row, and their dependent setup, branch result, and assertions. The audit must not repair records in place: return to pre-record KDT.4 transcription/evaluation, then rebuild before authoring. Using the exact final RuleCode setup, parameters, and matching simulation payload on the resolved invocation page, revalidate the complete mode-required proof set. In FIRST_MATCH, cellProof must cover consecutive rows from row 0 through the first true row, with every earlier row false; DTE.matched must contain exactly that first true row, or default only when every executable row has complete false proof. In EVALUATE_ALL, cellProof must cover every executable row and DTE.matched must equal, in stored order, all and only rows whose complete cellProof evaluates true; default is permitted only when that set is empty. DTE.actions and ordered DTA records must equal exactly the matched-row or applicable default actions, followed only in FIRST_MATCH with scalar enabled by the unique active allowed-result property actions selected by the final typed scalar through KDT.9. Reconstruct no OR row from pyOrConditions list order: use only the verified rawPyRowNum, ignore pyRowNum=-1, and reject any claimed row whose evidence changes or omits that association. Whenever a pyOrConditions entry has rawPyRowNum equal to the evaluated row, ignore that cell's pyCondition whether blank or populated and require the serialized input to satisfy at least one nonblank stored OR alternative; if the matching OR entry has no nonblank alternatives, the cell is wildcard true. Apply this rule identically in FIRST_MATCH and EVALUATE_ALL. If validation fails, invalidate the selected DT execution evidence and return before setup/assertion authoring to select a valid seed and rebuild DTE, DTA, setup, simulations, assertions, narratives, and RuleCode together. Never repair only assertions, and never accept Description, narrative, or ReasoningTrace text as row-selection proof.
A: During that revalidation, every claimed pyUseRange/pyUseRangeMax row pair must read both boundary values at the claimed row index and prove both comparisons against the same serialized typed input. A populated lower-bound cell is never wildcard because pyUseRange=true. If one input is intended to match multiple rows, it must satisfy the intersection of every claimed row range; equality to a strict lower bound fails that row. Any DTE, DTA chain, dependent caller result, or assertion based on a failed boundary is invalid and must be rebuilt with a satisfying seed before storage.
A: When DTE.scalar=NONE, require `OMIT|reason=inactive_evaluate_all_scalar` for DTM.mode=EVALUATE_ALL or `OMIT|reason=inactive_return_values_scalar` for DTM.mode=FIRST_MATCH with scalar disabled, for every recorded scalar consumer target. Forbid ASSERT, Not exists, blank/null/false substitution, scalar branch/result propagation, scalar-derived setup, and RuleCode expected results for those targets. Preserve independently FINAL DTA effects.
A: For every executing += DTA whose beforeSource requires setup, inspect final RuleCode.pySetupPages and require the exact DTA.page, DTA.target, and DTA.before value. Recompute the dependent expected result from that serialized before value and the ordered FINAL DTA chain. Missing or inconsistent setup invalidates only the dependent exact assertion until RuleCode is repaired.
A: When the caller requests a DT candidate for an exact downstream equality on a target produced by a deterministic += DTA chain, apply the KDT.11 inverse seed check before reporting that no compatible DT execution exists. If a valid typed numeric or nonblank Text/Identifier before value satisfies the constraint after complete DT re-evaluation, return that candidate to the caller workflow. Require the exact inverse before value in final pySetupPages and verify the serialized ordered DTA result equals the constrained final value. Never accept a fallback-only or ReasoningTrace-only incompatibility claim.
A: For every asserted property P, derive Decision Table write evidence only from FINAL DTA records referenced by the selected final DTE for that serialized test execution.
A: Treat independent setup or a pre-existing test input as existence proof only when the exact page-qualified key for P is physically present in the final serialized RuleCode input for the selected execution and resolved invocation page, including the applicable RuleCode.pySetupPages.pyPageDetails, serialized input row, or matching pySimulation payload. A planned seed, Description, InputsNarrative, AssertionsNarrative, ReasoningTrace statement, property definition, read of P, blank default action, or omitted JSON key is not setup or existence proof.
A: Inspect the actual serialized RuleCode inputs, executed caller/dependency writes, selected DTE/DTA records, and RuleCode.pyExpectedResults together. If no selected FINAL DTA targets P and no physical serialized input, earlier executed write, or later executed write creates P, require Write-Reachable(P,S)=false. If P is asserted, allow only `Not exists`; forbid `Is Empty`, `Exists`, and exact-value comparators, and rebuild the assertion before storage rather than accepting a narrative claim.
I: `Is Empty` is allowed only when existence of P is independently proved and its final value is blank. An omitted page-qualified key means absent, not empty.
A: For every serialized test affected by the table, require one selected DT-local DTE/DTA candidate consistent with final inputs and effects; reject disagreeing or duplicate raw variants. Report only execution-evidence validity to the caller, which owns test selection and layout. On failure, the audit invalidates affected records without patching them and returns to pre-record KDT.4 transcription/evaluation; that fresh phase may reread active fields and atomically rebuild DTM/DTA/DTE evidence, setup, assertions, and RuleCode, while inactive fields remain unread.